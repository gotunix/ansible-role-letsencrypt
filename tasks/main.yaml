---
# letsencrypt role — task orchestrator

- name: Install certbot
  ansible.builtin.import_tasks: install.yaml
  tags:
    - letsencrypt
    - install

- name: Issue and deploy certificates
  ansible.builtin.import_tasks: issue.yaml
  tags:
    - letsencrypt
    - issue
